# Windows桌面日程管理工具 - 开发文档

## 1. 项目概述

### 1.1 项目名称
桌面日程管理工具（DeskSchedule）

### 1.2 项目目标
开发一款轻量级Windows桌面日程管理工具，具备自动隐藏、鼠标触发显示、透明度调节等功能，提供灵活的提醒方式。

### 1.3 目标用户
需要在Windows桌面上快速记录和管理日程的用户，适合需要频繁查看但不想占用桌面空间的场景。

---

## 2. 功能需求

### 2.1 核心功能

#### 2.1.1 自动隐藏功能
- **触发条件**：用户停止编辑操作（键盘输入或鼠标点击）超过1分钟
- **隐藏方式**：窗口最小化到系统托盘
- **可配置项**：隐藏延迟时间（默认60秒）

#### 2.1.2 鼠标触发显示
- **触发区域**：
  - 屏幕左上角（坐标：x=0, y=0，检测范围50x50像素）
  - 屏幕右上角（坐标：x=ScreenWidth-50, y=0，检测范围50x50像素）
- **触发动作**：鼠标移入区域后，窗口从对应角落滑出显示
- **响应时间**：鼠标停留超过500ms后触发显示

#### 2.1.3 透明度调节
- **调节范围**：10% - 100%
- **调节方式**：通过设置面板滑块调节
- **实时预览**：调节时可实时预览效果
- **窗口置顶**：窗口始终置顶显示

#### 2.1.4 日程管理
- **基础功能**：添加、编辑、删除日程
- **日程属性**：
  - 标题（必填）
  - 详细描述
  - 日期时间
  - 提醒方式
  - 提醒状态
- **列表展示**：按时间排序显示所有日程

#### 2.1.5 提醒功能

##### 弹窗提醒
- **提醒样式**：Windows原生通知或自定义弹窗
- **自定义内容**：
  - 弹窗标题模板
  - 弹窗内容模板
  - 支持变量：{title}、{time}、{date}
- **弹窗位置**：屏幕右下角（可配置）
- **持续时间**：默认10秒后自动关闭

##### 声音提醒
- **默认声音**：提供3种内置提示音
- **自定义声音**：支持导入.wav或.mp3格式音频文件
- **音量控制**：可调节提醒音量（0-100%）
- **循环播放**：可选择是否循环播放直到用户确认

---

## 3. 技术选型

### 3.1 开发框架

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **C# + WPF** | 原生Windows体验、丰富的UI控件、良好的透明度支持 | 仅限Windows平台 | ★★★★★ |
| C# + WinForms | 简单易用 | UI较老旧、透明度实现复杂 | ★★★ |
| Electron | 跨平台 | 资源占用大、启动慢 | ★★ |
| Qt/C++ | 高性能 | 开发效率较低 | ★★★ |

**最终选择：C# + WPF**

### 3.2 技术栈详情

| 组件 | 技术选型 | 版本 |
|------|----------|------|
| 开发语言 | C# | .NET 6.0+ |
| UI框架 | WPF | .NET 6.0 |
| 数据库 | SQLite | 3.x |
| 依赖注入 | Microsoft.Extensions.DependencyInjection | 6.0+ |
| 日志框架 | NLog | 5.x |
| 单元测试 | xUnit | 2.x |

### 3.3 第三方库

| 库名 | 用途 |
|------|------|
| Hardcodet.NotifyIcon.Wpf | 系统托盘图标 |
| NAudio | 音频播放 |
| Newtonsoft.Json | 配置文件序列化 |

---

## 4. 系统架构

### 4.1 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                    Presentation Layer                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  MainWindow  │  │ SettingsView │  │ ScheduleView │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    ViewModel Layer                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ MainViewModel│  │SettingsVM    │  │ ScheduleVM   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    Service Layer                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │HideService   │  │TriggerService│  │ReminderService│ │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │AudioService  │  │NotifyService │                    │
│  └──────────────┘  └──────────────┘                    │
├─────────────────────────────────────────────────────────┤
│                    Data Layer                            │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │ScheduleRepo  │  │ ConfigRepo   │                    │
│  └──────────────┘  └──────────────┘                    │
│  ┌──────────────────────────────────┐                   │
│  │          SQLite Database         │                   │
│  └──────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────┘
```

### 4.2 模块划分

#### 4.2.1 核心模块

| 模块名 | 职责 | 类名 |
|--------|------|------|
| 自动隐藏模块 | 监测用户活动、计时隐藏 | `AutoHideService` |
| 鼠标触发模块 | 监测鼠标位置、触发显示 | `MouseTriggerService` |
| 透明度模块 | 窗口透明度管理 | `OpacityManager` |
| 日程管理模块 | CRUD操作 | `ScheduleService` |
| 提醒模块 | 弹窗/声音提醒 | `ReminderService` |

#### 4.2.2 辅助模块

| 模块名 | 职责 |
|--------|------|
| 配置管理模块 | 用户设置读写 |
| 数据访问模块 | SQLite数据库操作 |
| 日志模块 | 运行日志记录 |

---

## 5. 数据结构设计

### 5.1 数据库表结构

#### 5.1.1 日程表 (schedules)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY | 主键 |
| title | TEXT | NOT NULL | 日程标题 |
| description | TEXT | | 详细描述 |
| remind_time | DATETIME | NOT NULL | 提醒时间 |
| remind_type | INTEGER | DEFAULT 0 | 提醒类型：0=弹窗，1=声音，2=两者 |
| remind_sound | TEXT | | 自定义声音路径 |
| remind_text | TEXT | | 自定义提醒文本 |
| is_completed | INTEGER | DEFAULT 0 | 是否完成：0=否，1=是 |
| created_at | DATETIME | DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | | 更新时间 |

#### 5.1.2 配置表 (settings)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| key | TEXT | PRIMARY KEY | 配置键 |
| value | TEXT | NOT NULL | 配置值 |
| updated_at | DATETIME | | 更新时间 |

### 5.2 配置项定义

```json
{
  "hideDelaySeconds": 60,
  "triggerAreaSize": 50,
  "triggerDelayMs": 500,
  "windowOpacity": 0.9,
  "windowAlwaysOnTop": true,
  "reminderPopupEnabled": true,
  "reminderSoundEnabled": true,
  "reminderVolume": 80,
  "reminderSoundPath": "",
  "popupTitleTemplate": "日程提醒",
  "popupContentTemplate": "{title} - {time}",
  "popupDurationSeconds": 10
}
```

---

## 6. 核心功能实现设计

### 6.1 自动隐藏功能

```csharp
public class AutoHideService
{
    private DispatcherTimer _idleTimer;
    private DateTime _lastActivityTime;
    private int _hideDelaySeconds = 60;

    public void StartMonitoring()
    {
        _idleTimer = new DispatcherTimer();
        _idleTimer.Interval = TimeSpan.FromSeconds(1);
        _idleTimer.Tick += CheckIdleTime;
        _idleTimer.Start();

        // 监听全局输入事件
        InputManager.Current.PreProcessInput += OnInputEvent;
    }

    private void OnInputEvent(object sender, PreProcessInputEventArgs e)
    {
        _lastActivityTime = DateTime.Now;
    }

    private void CheckIdleTime(object sender, EventArgs e)
    {
        var idleTime = (DateTime.Now - _lastActivityTime).TotalSeconds;
        if (idleTime >= _hideDelaySeconds)
        {
            HideWindow();
        }
    }
}
```

### 6.2 鼠标触发功能

```csharp
public class MouseTriggerService
{
    private DispatcherTimer _checkTimer;
    private int _triggerAreaSize = 50;
    private int _triggerDelayMs = 500;
    private DateTime? _enterTime;

    public void StartMonitoring()
    {
        _checkTimer = new DispatcherTimer();
        _checkTimer.Interval = TimeSpan.FromMilliseconds(100);
        _checkTimer.Tick += CheckMousePosition;
        _checkTimer.Start();
    }

    private void CheckMousePosition(object sender, EventArgs e)
    {
        var position = GetMousePosition();
        var screenSize = GetScreenSize();

        bool inTopLeft = position.X < _triggerAreaSize && position.Y < _triggerAreaSize;
        bool inTopRight = position.X > screenSize.Width - _triggerAreaSize && position.Y < _triggerAreaSize;

        if (inTopLeft || inTopRight)
        {
            if (_enterTime == null)
                _enterTime = DateTime.Now;
            else if ((DateTime.Now - _enterTime.Value).TotalMilliseconds >= _triggerDelayMs)
                ShowWindow(inTopLeft ? Corner.Left : Corner.Right);
        }
        else
        {
            _enterTime = null;
        }
    }
}
```

### 6.3 透明度调节

```xaml
<!-- XAML -->
<Window x:Class="DeskSchedule.MainWindow"
        AllowsTransparency="True"
        WindowStyle="None"
        Topmost="True">
    <Grid Background="White" Opacity="{Binding WindowOpacity}">
        <!-- 内容 -->
    </Grid>
</Window>
```

### 6.4 提醒功能

```csharp
public class ReminderService
{
    private readonly INotifyService _notifyService;
    private readonly IAudioService _audioService;

    public async Task TriggerReminder(Schedule schedule)
    {
        var tasks = new List<Task>();

        if (schedule.RemindType.HasFlag(RemindType.Popup))
        {
            tasks.Add(_notifyService.ShowNotification(
                ReplaceTemplate(schedule.PopupTitleTemplate, schedule),
                ReplaceTemplate(schedule.PopupContentTemplate, schedule)
            ));
        }

        if (schedule.RemindType.HasFlag(RemindType.Sound))
        {
            tasks.Add(_audioService.PlaySound(
                schedule.RemindSoundPath,
                schedule.RemindVolume
            ));
        }

        await Task.WhenAll(tasks);
    }
}
```

---

## 7. 用户界面设计

### 7.1 主界面布局

```
┌─────────────────────────────────────┐
│  📅 日程管理              [─][□][×] │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │ 🔍 搜索日程...              │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ 今天                        │    │
│  │ ├─ 09:00 会议讨论           │    │
│  │ └─ 14:00 项目评审           │    │
│  ├─────────────────────────────┤    │
│  │ 明天                        │    │
│  │ └─ 10:00 团队周会           │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │        [+ 添加日程]         │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  透明度: ────●────────── 90%  ⚙设置│
└─────────────────────────────────────┘
```

### 7.2 设置界面

```
┌─────────────────────────────────────┐
│  ⚙ 设置                     [×]    │
├─────────────────────────────────────┤
│                                     │
│  ▶ 自动隐藏                         │
│    隐藏延迟: [60] 秒                │
│                                     │
│  ▶ 鼠标触发                         │
│    触发区域大小: [50] 像素          │
│    触发延迟: [500] 毫秒             │
│                                     │
│  ▶ 窗口设置                         │
│    透明度: ────●────────── 90%     │
│    □ 始终置顶                       │
│                                     │
│  ▶ 提醒设置                         │
│    ☑ 启用弹窗提醒                   │
│    弹窗标题模板: [日程提醒]         │
│    弹窗内容模板: [{title} - {time}] │
│    弹窗持续时间: [10] 秒            │
│                                     │
│    ☑ 启用声音提醒                   │
│    音量: ────────●── 80%           │
│    提示音: [默认提示音] [浏览...]   │
│                                     │
│         [保存设置]  [取消]          │
└─────────────────────────────────────┘
```

---

## 8. 项目结构

```
DeskSchedule/
├── DeskSchedule.sln
├── DeskSchedule/
│   ├── App.xaml
│   ├── App.xaml.cs
│   ├── MainWindow.xaml
│   ├── MainWindow.xaml.cs
│   ├── Views/
│   │   ├── SettingsView.xaml
│   │   ├── SettingsView.xaml.cs
│   │   ├── ScheduleEditView.xaml
│   │   └── ScheduleEditView.xaml.cs
│   ├── ViewModels/
│   │   ├── MainViewModel.cs
│   │   ├── SettingsViewModel.cs
│   │   └── ScheduleEditViewModel.cs
│   ├── Services/
│   │   ├── IAutoHideService.cs
│   │   ├── AutoHideService.cs
│   │   ├── IMouseTriggerService.cs
│   │   ├── MouseTriggerService.cs
│   │   ├── IReminderService.cs
│   │   ├── ReminderService.cs
│   │   ├── INotifyService.cs
│   │   ├── NotifyService.cs
│   │   ├── IAudioService.cs
│   │   └── AudioService.cs
│   ├── Models/
│   │   ├── Schedule.cs
│   │   ├── AppSettings.cs
│   │   └── RemindType.cs
│   ├── Data/
│   │   ├── AppDbContext.cs
│   │   ├── IScheduleRepository.cs
│   │   ├── ScheduleRepository.cs
│   │   ├── IConfigRepository.cs
│   │   └── ConfigRepository.cs
│   ├── Helpers/
│   │   ├── MouseHelper.cs
│   │   └── TemplateHelper.cs
│   ├── Resources/
│   │   ├── Sounds/
│   │   │   ├── default.wav
│   │   │   ├── chime.wav
│   │   │   └── bell.wav
│   │   └── Styles/
│   │       └── AppStyles.xaml
│   └── Assets/
│       └── tray_icon.ico
├── DeskSchedule.Tests/
│   ├── Services/
│   │   ├── AutoHideServiceTests.cs
│   │   └── ReminderServiceTests.cs
│   └── Data/
│       └── ScheduleRepositoryTests.cs
└── docs/
    └── user-guide.md
```

---

## 9. 开发计划

### 9.1 里程碑规划

| 阶段 | 内容 | 预计周期 |
|------|------|----------|
| Phase 1 | 项目搭建、基础UI框架 | 3天 |
| Phase 2 | 日程管理核心功能（CRUD） | 5天 |
| Phase 3 | 自动隐藏功能 | 2天 |
| Phase 4 | 鼠标触发显示功能 | 2天 |
| Phase 5 | 透明度调节功能 | 1天 |
| Phase 6 | 提醒功能（弹窗+声音） | 4天 |
| Phase 7 | 设置界面与配置持久化 | 2天 |
| Phase 8 | 测试与优化 | 3天 |
| **总计** | | **22天** |

### 9.2 详细任务分解

#### Phase 1: 项目搭建 (Day 1-3)
- [ ] 创建WPF项目结构
- [ ] 配置依赖注入容器
- [ ] 设置SQLite数据库
- [ ] 实现基础MVVM框架
- [ ] 创建主窗口布局

#### Phase 2: 日程管理核心 (Day 4-8)
- [ ] 设计数据库表结构
- [ ] 实现ScheduleRepository
- [ ] 创建日程列表界面
- [ ] 实现添加日程功能
- [ ] 实现编辑日程功能
- [ ] 实现删除日程功能
- [ ] 实现日程搜索功能

#### Phase 3: 自动隐藏 (Day 9-10)
- [ ] 实现用户活动监听
- [ ] 实现空闲计时器
- [ ] 实现窗口隐藏逻辑
- [ ] 实现系统托盘图标
- [ ] 添加配置项支持

#### Phase 4: 鼠标触发 (Day 11-12)
- [ ] 实现鼠标位置监听
- [ ] 实现触发区域检测
- [ ] 实现触发延迟逻辑
- [ ] 实现窗口显示动画
- [ ] 添加配置项支持

#### Phase 5: 透明度调节 (Day 13)
- [ ] 实现窗口透明度设置
- [ ] 创建透明度调节控件
- [ ] 实现实时预览
- [ ] 添加配置项支持

#### Phase 6: 提醒功能 (Day 14-17)
- [ ] 实现提醒调度服务
- [ ] 集成Windows通知API
- [ ] 实现NAudio音频播放
- [ ] 实现自定义声音导入
- [ ] 实现模板替换逻辑
- [ ] 添加音量控制

#### Phase 7: 设置界面 (Day 18-19)
- [ ] 创建设置界面
- [ ] 实现配置读写
- [ ] 实现设置项绑定
- [ ] 添加重置默认功能

#### Phase 8: 测试优化 (Day 20-22)
- [ ] 编写单元测试
- [ ] 集成测试
- [ ] 性能优化
- [ ] 内存泄漏检查
- [ ] 打包发布

---

## 10. 风险与对策

| 风险 | 可能性 | 影响 | 对策 |
|------|--------|------|------|
| 鼠标钩子影响系统性能 | 中 | 高 | 使用低频率轮询替代全局钩子 |
| 音频播放兼容性问题 | 低 | 中 | 使用NAudio库处理多种格式 |
| Windows版本兼容性 | 中 | 高 | 明确支持Windows 10/11，向下兼容测试 |
| 透明度影响性能 | 低 | 低 | 使用硬件加速渲染 |

---

## 11. 发布计划

### 11.1 打包方式
- 使用MSIX或ClickOnce打包
- 生成安装程序（.exe或.msi）
- 支持便携版（免安装）

### 11.2 系统要求
- 操作系统：Windows 10 (1809+) / Windows 11
- 运行时：.NET 6.0 Runtime
- 磁盘空间：50MB

---

## 12. 后续迭代规划

### v1.1
- 日程分类与标签
- 日程重复功能
- 日历视图

### v1.2
- 云同步功能
- 多设备同步
- 数据导入导出

### v2.0
- AI智能提醒
- 自然语言输入
- 日程分析统计
